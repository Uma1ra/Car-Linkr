<div class="container">
  <h3>お問い合わせフォーム</h3>
  <p>ゲストユーザーの方は連絡先も含めてご記入ください</p>
  <div class="d-flex justify-content-center">
    <%= form_with model: @enquiry do |f| %>
      <%= render "layouts/errors", obj: @enquiry %>
      <%= f.hidden_field :customer_id, value: current_customer.id %>
      <div class="form-group row">
        <%= f.label :title, "タイトル" %>
        <%= f.text_field :title, autofocus: true %>
      </div>
      <div class="form-group row">
        <%= f.label :detail, "内容" %>
        <%= f.text_area :detail , rows: "7", placeholder: "お問い合わせ内容をご記入ください", autofocus: true %>
      </div>
      <div>
        <%= f.submit "送信" %>
      </div>
    <% end %>
  </div>
  
</div>