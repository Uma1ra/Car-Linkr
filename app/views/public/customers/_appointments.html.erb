<% if current_customer.appointments.present? %>
  <table class="table table-bordered">
    <thead>
      <tr>
        <th>予約ID</th>
        <th>予約申込日時</th>
        <th>予約内容</th>
        <th>予約状況</th>
        <th>詳細へのリンク</th>
      </tr>
    </thead>
    <tbody>
      <% appointments.each do |app| %>
        <tr>
          <td><%= app.id %></td>
          <td><%= l app.created_at, format: :long %></td>
          <td><%= app.category_i18n %></td>
          <td class="<%= app.is_done ? "text-success" : "text-danger" %>">
            <%= app.is_done ? "対応済" : "未対応" %>
          </td>
          <td>
            <%= link_to "詳細", appointment_path(app.id), class: "btn btn-primary btn-sm btn-block" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <div class="d-flex justify-content-center">
    <%= paginate appointments %>
  </div>
<% else %>
  <h4 class="text-center">まだ予約情報はございません</h4>
<% end %>